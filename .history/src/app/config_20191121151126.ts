// const SERVER_URL = 'HTTP://localhost';
const SERVER_URL = 'https://restcountries';

// const PORT = '4200';
const PORT = 'eu';
// export const LOGIN_URL = SERVER_URL + ':' + PORT + '/login'

export const config = {
  production: true,
  // LOGIN_URL : SERVER_URL + '.' + PORT + '/rest/v2/all'
  // LOGIN_URL : `https://restcountries.eu/rest/v2/all`
  
};
